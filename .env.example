# Vercel KV 설정 (배포 시 자동 설정됨)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# 로컬 개발 시에는 이 값들이 없으면 메모리 Map 사용
# Vercel 배포 시에는 자동으로 KV 환경변수가 설정됨

# Vercel 배포를 위한 기본 URL (선택사항)
NEXT_PUBLIC_BASE_URL=

# 개발 환경 설정
NODE_ENV=development